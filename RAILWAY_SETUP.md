# 🚀 Настройка для Railway

## ✅ Проверка безопасности

Все чувствительные данные уже убраны из кода и используют переменные окружения.

## 📋 Переменные окружения для Railway

Скопируйте эти переменные в **Railway → Settings → Variables**:

### Обязательные переменные:

```
TELEGRAM_TOKEN=ваш_токен_бота
YOUTUBE_API_KEY=ваш_youtube_api_ключ
ADMIN_ID=ваш_telegram_id
```

### Опциональные переменные:

```
YOUTUBE_API_KEY_2=ваш_второй_ключ_опционально
DATABASE_PATH=youtube_tracker.db
```

## 📝 Как получить значения:

### 1. TELEGRAM_TOKEN
- Откройте [@BotFather](https://t.me/BotFather)
- Команда `/newbot` или `/token` для существующего бота
- Скопируйте токен

### 2. YOUTUBE_API_KEY
- [Google Cloud Console](https://console.cloud.google.com/)
- Создайте проект → Включите YouTube Data API v3
- Credentials → Create API Key
- Скопируйте ключ

### 3. ADMIN_ID
- Откройте [@userinfobot](https://t.me/userinfobot)
- Команда `/start`
- Скопируйте ваш ID (число)

## 🔍 Проверка после деплоя

В логах Railway должно быть:
- ✅ `Telegram Token: Set`
- ✅ `YouTube API Key: Set`
- ✅ `Admin ID: Set`

## ⚠️ Важно

- **НЕ коммитьте** `.env` файлы в git
- Все переменные только в Railway Settings
- После добавления переменных → перезапустите сервис

## 📁 Файлы проекта

- `bot.py` - основной файл бота
- `config.py` - конфигурация (использует переменные окружения)
- `channels_config.json` - список каналов (уже настроен)
- `Procfile` - настройка для Railway
- `requirements.txt` - зависимости

## 🎯 Готово к деплою!

После добавления переменных окружения в Railway, бот автоматически:
- Запустится при деплое
- Будет отправлять ежедневные отчеты в 11:00 МСК
- Отслеживать все настроенные каналы
